<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Gradient background -->
    <gradient
        android:startColor="#CCFFB5E7"
        android:endColor="#CCFF9A9E"
        android:angle="90" />

    <!-- Border (stroke) -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" />

    <!-- Rounded corners -->
    <corners android:radius="20dp" />
</shape>